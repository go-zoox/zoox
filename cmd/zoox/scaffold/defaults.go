package scaffold

// DefaultAppPackage is the main package to build and run for a project created with `zoox new`.
const DefaultAppPackage = "./cmd/server"

// DefaultOutputBinary is the default -o path for `zoox build` and the suggested output name.
const DefaultOutputBinary = "./bin/server"
